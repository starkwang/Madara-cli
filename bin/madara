#!/usr/bin/env node
madara = require('madara');
program = require('commander');

program
  .version('0.0.1')
  .option('-a, --all','Add all')
  .parse(process.argv);

if(program.all){
  madara.constructAll();
}

// madara.constructAll();
